# Native source recovery boundary

This directory is the source path proven by `classes3.dex` metadata for package `com.aurum.bistterminal8`.

Proven original Kotlin filenames:
`AurumScheduler.kt`, `BootReceiver.kt`, `MainActivity.kt`, `NativeMarketHttp.kt`, `NativeOpenAI.kt`, `PipelineService.kt`, `SchedulerLedger.kt`, `SecureSecretStore.kt`, `TriggerReceiver.kt`.

The original Kotlin text has not yet been recovered from a matching historical GitHub object. Do not place guessed code here without a RECONSTRUCTED marker.
